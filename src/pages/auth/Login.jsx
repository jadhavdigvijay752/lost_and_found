import React from "react";
import LoginComponent from "../../components/auth/LoginComponent";

/**
 * Login component that renders the LoginComponent.
 *
 * @component
 * @returns {JSX.Element} The rendered login component.
 */
function Login() {
  return (
    <div>
      <LoginComponent />
    </div>
  );
}

export default Login;
